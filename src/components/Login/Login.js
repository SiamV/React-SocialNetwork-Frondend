import React from "react";
import classes from "./Login.module.css"
const Login = (props) => {
    return (
        <div className={classes.loginForm}>Login form will be Here</div>
    )
}

export default Login;