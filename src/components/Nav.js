import React from "react";

const Nav = () => {
    return (
        <nav className={'free-nav'}>
            <a>Home</a>
            <a>Groups</a>
            <a>Country</a>
        </nav>
    );
}

export default Nav;