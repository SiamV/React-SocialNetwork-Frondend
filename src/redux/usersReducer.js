let stateDefault = {
    users: "1"
}

const usersReducer = (state = stateDefault, action) => {
    switch (action.type) {

        default:
            return state;
    }

}

export default usersReducer;